# Test-only canary pair. Shadows the (blank) generated main resource on the test classpath so the
# baked-resolution and never-persisted tests have observable values. Never real; never shipped.
url=https://baked-canary.test
token=BAKED_CANARY_TOKEN_00000000
